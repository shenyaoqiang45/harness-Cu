---
description: 报告更新后自动 commit 并 push 到 origin
alwaysApply: true
---

# 报告更新后 Push

完成伦敦铜预测报告更新（`python -m copper_forecast.cli run` / `fetch` + `report`，或事件驱动手工改数后重跑）后，**在同一轮工作内**提交并推送，不要只生成本地报告。

## 触发条件

- 生成了新的 `reports/runs/live_*.md`
- 或更新了报告依赖的数据：`data/raw/`、`data/validated/`、`data/clean/`、`data/audit/`
- 或更新了 `data/raw/supply_events.csv`、`data/raw/manual_indicators.csv`、`checklist.md`

## 标准流程

1. 确认 `cli run` 成功，新报告路径已知。
2. 复核报告更新检查项（见 `checklist.md`）。
3. **仅 stage 本次报告链路相关文件**（勿 `git add .` 扫入无关 probe 脚本或未请求改动）：
 - `reports/runs/live_*.md`（本次新生成的）
 - `data/raw/`、`data/validated/latest.csv`、`data/clean/latest.csv`、`data/audit/`、`data/forecast/`
 - 若本次有改：`data/raw/supply_events.csv`、`data/raw/manual_indicators.csv`、`checklist.md`
4. Commit（一句说明「为何」）：

```bash
git commit -m "report: live YYYY-MM-DD <触发事件或数据源简述>"
```

5. **立即 push**：

```bash
git push origin HEAD
```

## 禁止

- 不提交 `.env`、密钥、本地编辑器配置。
- 不在用户明确说「只本地、不要 push」时推送。
- 不 force push `main`/`master`。
- 不 amend 已 push 的 commit（除非用户明确要求）。

## Commit 信息示例

- `report: live 2026-07-31 china PMI + cli run`
- `report: live 2026-07-29 post-FOMC fetch and run`
- `report: supply_events 232 signed confidence A`
